{
  "name": "dpipemazo-wedsite",
  "description": "Django Wedding Site by dpipemazo",
  "scripts": {
  },
  "env": {
    "DJANGO_SECRET_KEY" : {
      "required": true
    },
    "GOOGLE_MAPS_API_KEY" : {
      "required": true
    },
    "DEBUG" : "True"
  },
  "formation": {
  },
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ],
  "scripts" : {
      "postdeploy": "python manage.py migrate"
  }
}
