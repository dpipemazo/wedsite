{
  "name": "wedding_site",
  "description": "Jennifer and Dan's Wedding Site!",
  "scripts": {
  },
  "env": {
    "DJANGO_SECRET_KEY" : {
      "required": true
    },
    "GOOGLE_MAPS_API_KEY" : {
      "required": true
    },
    "MAILGUN_SMTP_LOGIN" : {
      "required": true
    },
    "MAILGUN_SMTP_PASSWORD" : {
      "required": true
    },
    "MAILGUN_SMTP_PORT" : {
      "required": true
    },
    "MAILGUN_SMTP_SERVER" : {
      "required": true
    },
    "DEBUG" : "True"
  },
  "formation": {
  },
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ],
  "scripts" : {
      "postdeploy": "python manage.py migrate"
  }
}
